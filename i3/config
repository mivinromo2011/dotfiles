#Mod keys
set $mod Mod4
set $mod2 Mod1
font pango:Source Code Pro 12

#gaps
set $inner_gaps 7
set $outer_gaps 7
default_border pixel 0
default_floating_border pixel 2
for_window [class="Termite"] border pixel 2
gaps inner $inner_gaps
gaps outer $outer_gaps

#Window switch
bindsym $mod2+Tab exec rofi -show window -lines 7 -eh 2 -bw 2 -font "MesloLGS NF 13" -show-icons -icon-theme="Papirus"

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && pkill -RTMIN+10 i3blocks
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && pkill -RTMIN+10 i3blocks
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && pkill -RTMIN+10 i3blocks
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && pkill -RTMIN+10 i3blocks

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+q kill

# start rofi
bindsym $mod2+e exec "rofi -show drun -lines 7 -eh 2 -bw 2 -font 'MesloLGS NF 13' -icon-theme 'Arc' -show-icons"

# change focus
bindsym $mod+a focus left
bindsym $mod+s focus down
bindsym $mod+w focus up
bindsym $mod+d focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+a move left
bindsym $mod+Shift+s move down
bindsym $mod+Shift+w move up
bindsym $mod+Shift+d move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+Shift+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod2+t layout stacking
bindsym $mod2+g layout tabbed
bindsym $mod2+f  layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+Tab focus mode_toggle

# focus the parent container
#bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
#Screen1
set $ws1 "1: Face "
set $ws2 "2: Code "
set $ws3 "3: VirtualBox "
set $ws4 "4: Notion "
set $ws5 "5: Workshop2 "    
set $ws11 "11: Watch "



#Screen2
set $ws6 "6: Pad "
set $ws7 "7: Music "
set $ws8 "8: Panel "
set $ws9 "9: Workshop "
set $ws10 "10: Video-Rec "



# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2 
bindsym $mod+3 workspace number $ws3 ; layout tabbed
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws11 ; layout tabbed

 
bindsym $mod2+1 workspace number $ws6 ; layout tabbed
bindsym $mod2+2 workspace number $ws7
bindsym $mod2+3 workspace number $ws8
bindsym $mod2+4 workspace number $ws9
bindsym $mod2+5 workspace number $ws10 


# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws11

bindsym $mod2+Shift+1 move container to workspace number $ws6
bindsym $mod2+Shift+2 move container to workspace number $ws7
bindsym $mod2+Shift+3 move container to workspace number $ws8
bindsym $mod2+Shift+4 move container to workspace number $ws9
bindsym $mod2+Shift+5 move container to workspace number $ws10



# reload the configuration file
bindsym $mod+Ctrl+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"
#Window Colors:
set $bg_color 	        #d8dee9
set $bg_color_bar 	#2e3440  
set $urgcolor  	        #bf616a           
set $iac_bg_color       #a3be8c

#			Border		Background		text 		indicator
client.focused		$urgcolor 	$urgcolor	      $bg_color 	$iac_bg_color
client.unfocused 	$bg_color 	$bg_color             $urgcolor         $bg_color
client.focused_inactive $bg_color       $bg_color             $urgcolor         $iac_bg_color
client.urgent 		$urgcolor	$urgcolor	      $bg_color 	$urgcolor
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        
        position top
        font pango:DejaVu Sans Mono 12
        #status_command i3status
	#position top
        height 23
        strip_workspace_numbers yes
        workspace_min_width 40
	tray_output none
	status_command i3blocks -c ~/.config/i3blocks/i3blocks.conf
	colors {
                background      $bg_color_bar
                statusline      $bg_color
                separator       $bg_color
	#		   Border 	  BG     	 Text
        focused_workspace  $bg_color     $urgcolor	$bg_color
        active_workspace   $urgcolor     $bg_color 	$urgcolor
        inactive_workspace $bg_color_bar $bg_color_bar  $bg_color
        urgent_workspace   $bg_color_bar $urgcolor 	$bg_color
        binding_mode       $bg_color_bar $urgcolor 	$bg_color
     }
}


#AutoStart Programs
exec_always feh --bg-scale ~/Vault/setup/Wallpapers/arc.png
exec_always picom -f
exec bitwarden
exec termite 
exec notion-app
exec pavucontrol
exec blueberry
exec arandr
exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1



#onedrive mount
exec rclone --vfs-cache-mode writes mount "College-Ondrive":  ~/College



#Assigning workspaces to screens
workspace $ws1 output Primary
workspace $ws2 output Primary
Workspace $ws3 output Primary
workspace $ws4 output Primary
Workspace $ws5 output Primary
 
workspace $ws6 output eDP1
workspace $ws7 output eDP1
workspace $ws8 output eDP1
workspace $ws9 output eDP1
workspace $ws10 output eDP1


# screen setup
exec xrandr --output eDP1 --mode 1920x1080 --pos 1920x0 --rotate normal --output DP-1 --off --output HDMI1 --primary --mode 1920x1080 --pos 0x0 --rotate normal


#My key Binds
bindsym $mod+z          exec flameshot gui
bindsym $mod+x          [class="Termite"] scratchpad show
bindsym $mod+e          exec pcmanfm
bindsym $mod+p          exec arandr
bindsym $mod+Shift+b    exec chromium
bindsym $mod+Shift+z    [class="Bitwarden"] scratchpad show
bindsym $mod+b          exec firefox
bindsym $mod+v          exec virtualbox -style kvantum-dark U%
bindsym $mod+Shift+x    exec spotify
bindsym $mod+m          exec --no-startup-id termite -e "bashtop"
bindsym $mod+n          exec notion-app
bindsym $mod+c          exec code

bindsym $mod2+Shift+v   exec "pcmanfm /home/mivin/Vault"
bindsym $mod2+v         exec pavucontrol
bindsym $mod2+n         exec --no-startup-id termite -e "nmtui"
bindsym $mod2+b         exec blueberry
bindsym $mod+Shift+c    exec "code .config/i3/config"
bindsym $mod2+c         [class="TelegramDesktop"] scratchpad show
bindsym $mod2+Shift+b   [title="WhatsApp*"] scratchpad show

#Screen Lock
bindsym $mod+Ctrl+l exec dm-tool lock

#Display workspace move
bindsym $mod+Ctrl+1 move workspace to output HDMI1
bindsym $mod+Ctrl+2 move workspace to output eDP1

# Sreen brightness controls
bindsym $mod2+Ctrl+w exec --no-startup-id xbacklight -inc 10
bindsym $mod2+Ctrl+s exec --no-startup-id xbacklight -dec 10

#gaps
bindsym $mod+i			gaps inner current plus 5
bindsym $mod+Shift+i		gaps inner current minus 5
bindsym $mod+o			gaps outer current plus 5
bindsym $mod+Shift+o		gaps outer current minus 5

#Brightness
bindsym XF86MonBrightnessDown	exec --no-startup-id xbacklight -dec 10
bindsym XF86MonBrightnessUp	exec --no-startup-id xbacklight -inc 10

#Screen Layout Switch
#bindsym $mod2+Ctrl+q exec "xrandr --output eDP1 --mode 192x1080 --pos 0x1080 --rotate normal --output DP1 --off --output HDMI1 --primary --mode 1920x1080 --pos 0x0 --rotate normal --output VIRTUAL1 --off" 

#audio
bindsym $mod2+w                 exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && pkill -RTMIN+10 i3blocks
bindsym $mod2+s                 exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && pkill -RTMIN+10 i3blocks
bindsym $mod2+Shift+s           exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && pkill -RTMIN+10 i3blocks
#bindsym XF86AudioMicMute       exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && pkill -RTMIN+10 i3blocks

#Media
bindsym $mod2+z exec playerctl play-pause
bindsym $mod2+d exec playerctl next 
bindsym $mod2+a exec playerctl previous 

#window rules

for_window [class="Galculator"] floating enable
for_window [class="feh"] floating enable 
for_window [class="Lxappearance"] floating enable
for_window [class="qpdfview"] floating enable
for_window [class="Sxiv"] floating enable resize 800 600
for_window [class="mpv"] floating enable
for_window [title="Microsoft Teams Notification"] floating enable
for_window [title="VirtualBox"] tabbed enable
for_window [title="Windows - Soft Keyboard"] floating enable
for_window [title=" BashTOP"] floating enable
for_window [title="Polkit-gnome-authentication-agent-1"] floating enable resize 226 126

#Workspace rules

for_window [class="VirtualBox Manager"] move to workspace $ws3
for_window [class="VirtualBox Machine"] move to workspace $ws3
for_window [class="discord"] move to workspace $ws10
for_window [class="obs"] move to workspace $ws10
for_window [class="code-oss"] move to workspace $ws2
for_window [class="Bitwarden"] move scratchpad
for_window [class="Spotify"] move to workspace $ws7
for_window [class="Transmission"] move to workspace $ws8
for_window [class="Termite"] move scratchpad
for_window [class="TelegramDesktop"] move scratchpad 
for_window [class="Notion"] move to workspace $ws4
for_window [class="Blueberry"] move to workspace $ws8
for_window [class="Arandr"] move to workspace $ws8
for_window [class="Microsoft Teams - Preview"] move to workspace $ws6
for_window [title="Microsoft Teams Notification"] move to workspace $ws6
for_window [class="Pavucontrol"] move to workspace $ws8

#Scratchpads
bindsym $mod+backslash scratchpad show
bindsym $mod+Shift+backslash move scratchpad


