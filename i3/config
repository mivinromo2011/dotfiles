set $mod Mod4
set $mod2 Mod1

font pango:DejaVu Sans 14

set $inner_gaps 7
set $outer_gaps 7
gaps inner $inner_gaps
gaps outer $outer_gaps
default_border pixel
default_floating_border pixel 2
popup_during_fullscreen smart
bindsym $mod+Shift+t            gaps inner current set $inner_gaps; gaps outer current set $outer_gaps

bindsym $mod2+i                  gaps inner current plus 5
bindsym $mod2+Shift+i            gaps inner current minus 5
bindsym $mod2+o                  gaps outer current plus 5
bindsym $mod2+Shift+o            gaps outer current minus 5

exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# Use pactl to adjust volume in PulseAudio.
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && pkill -RTMIN+10 i3blocks
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && pkill -RTMIN+10 i3blocks
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && pkill -RTMIN+10 i3blocks

#Brightness
bindsym XF86MonBrightnessDown   exec --no-startup-id xbacklight -dec 10 && pkill -RTMIN+13 i3blocks
bindsym XF86MonBrightnessUp     exec --no-startup-id xbacklight -inc 10 && pkill -RTMIN+13 i3blocks

floating_modifier $mod
bindsym $mod+q kill

# start rofi-menu
bindsym $mod+d exec --no-startup-id dmenu_run -fn 'Noto Sans-14'

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+i layout stacking
bindsym $mod+o layout tabbed
bindsym $mod+p layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

bindsym $mod+Tab workspace back_and_forth

# Workspaces
set $ws1 "1:  "
set $ws2 "2:  "
set $ws3 "3:  "
set $ws4 "4: ﬏ "
set $ws5 "5:  "
set $ws6 "6:  "
set $ws7 "7:  "
set $ws8 "8:  "
set $ws9 "9:  "
set $ws10 "10: 阮 "
set $ws11 "11: S1 "
set $ws12 "12: S2 "
set $Ws13 "13: S3 "
set $Ws14 "14: S4 "
set $Ws15 "15: S5 "


# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3 ; layout tabbed
bindsym $mod+4 workspace number $ws4 ; layout tabbed
bindsym $mod+5 workspace number $ws5
bindsym $mod2+1 workspace number $ws6
bindsym $mod2+2 workspace number $ws7
bindsym $mod2+3 workspace number $ws8 ; layout tabbed
bindsym $mod2+4 workspace number $ws9
bindsym $mod2+5 workspace number $ws10
bindsym $mod+6 workspace number $ws11
bindsym $mod+7 workspace number $ws12
bindsym $mod+8 workspace number $ws13
bindsym $mod+9 workspace number $ws14
bindsym $mod+0 workspace number $ws15


# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1 ; workspace $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2 ; workspace $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3 ; workspace $ws3 ; layout tabbed
bindsym $mod+Shift+4 move container to workspace number $ws4 ; workspace $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5 ; workspace $ws5
bindsym $mod2+Shift+1 move container to workspace number $ws6 ; workspace $ws6
bindsym $mod2+Shift+2 move container to workspace number $ws7 ; workspace $ws7
bindsym $mod2+Shift+3 move container to workspace number $ws8 ; workspace $ws8 ; layout tabbed
bindsym $mod2+Shift+4 move container to workspace number $ws9 ; workspace $ws9
bindsym $mod2+Shift+5 move container to workspace number $ws10 ; workspace $ws10
bindsym $mod+Shift+6 move container to workspace number $ws11 ; workspace $ws11
bindsym $mod+Shift+7 move container to workspace number $ws12 ; workspace $ws12
bindsym $mod+Shift+8 move container to workspace number $ws13 ; workspace $ws13
bindsym $mod+Shift+9 move container to workspace number $ws14 ; workspace $ws14
bindsym $mod+Shift+0 move container to workspace number $ws15 ; workspace $ws15


#window Resize
bindsym $mod2+Left    resize grow width 10 px or 10 ppt
bindsym $mod2+Right   resize shrink width 10 px or 10 ppt
bindsym $mod2+Up      resize grow height 10 px or 10 ppt
bindsym $mod2+Down    resize shrink height 10 px or 10 ppt

#Assigning workspaces to screens
workspace $ws1 output primary
workspace $ws2 output primary
workspace $ws3 output primary
workspace $ws4 output primary
workspace $ws5 output primary
workspace $ws7 output eDP1
workspace $ws8 output eDP1
workspace $ws9 output eDP1
workspace $ws10 output eDP1

#Workspace rules
for_window [class="LibreWolf"] move to workspace $ws1
for_window [class="firefox"] move to workspace $ws2
for_window [class="VirtualBox Manager"] move to workspace $ws3
for_window [class="VirtualBox Machine"] move to workspace $ws3
for_window [class="code-oss"] move to workspace $ws4
for_window [class="kitty"] move to workspace $ws5
for_window [class="Notion"] move to workspace $ws6
for_window [class="FreeTube"] move to workspace $ws7
for_window [class=discord|Ferdi] move to workspace $ws8
for_window [class="Microsoft Teams - Preview"] move to workspace $ws8
for_window [class="Transmission"] move to workspace $ws9
for_window [class="Spotify"] move to workspace $ws10

# reload the configuration file
bindsym $mod+Ctrl+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -f hack -t warning -m 'Power Menu' -b 'ShutDown' 'systemctl poweroff' -b 'Restart' 'systemctl reboot' -B 'Logout' 'i3-msg exit'"
bindsym $mod+Shift+c exec "i3-nagbar -f hack -t warning -m 'Control Panel' -b 'Edit Config' 'code ~/.config/i3/config' -B 'Volume Contorl' 'raiseorlaunch -c Pavucontrol -r' -B 'Appearance' 'raiseorlaunch -c Lxappearance'"

set_from_resource $black_back   black_back
set_from_resource $grey_back    grey_back
set_from_resource $blue_highl   blue_highl
set_from_resource $red_urg      red_urg
set_from_resource $text_white   text_white
set_from_resource $green_highl  green_highl
set_from_resource $dark_black   dark_black
set_from_resource $light_black  light_black

bar {
 	position top
        status_command i3blocks -c ~/.config/i3blocks/i3blocks.conf
        tray_padding 5
        tray_output none
        strip_workspace_numbers true
        workspace_min_width 50
        height 28
        colors{
                background $black_back
                statusline $text_white
                separator  $text_white

                #                  border       backgr       text   
                focused_workspace  $light_black $blue_highl  $text_white
                active_workspace   $light_black $grey_back   $text_white
                inactive_workspace $dark_black  $light_black $text_white
                urgent_workspace   $dark_black  $red_urg     $text_white
                binding_mode       $dark_black  $red_urg     $text_white
        }
}

#Window colours         border          backgr.         text            indicator       child_border
client.focused          $dark_black     $blue_highl     $text_white     $green_highl    $blue_highl
client.focused_inactive $dark_black     $black_back      $text_white     $green_highl    $blue_highl
client.unfocused        $dark_black     $grey_back      $black_back     $green_highl    $grey_back
client.urgent           $red_urg        $red_urg        $text_white     $green_highl    $grey_back
client.placeholder      $black_back     $grey_back      $text_white     $green_highl    $grey_back
client.background       $text_white

#AutoStart
exec --no-startup-id redshift -l 18.007202:79.558296
exec_always feh --bg-fill /home/mivin/Setup/Wallpapers/punk.jpg
exec bitwarden
exec firefox
exec pavucontrol
exec librewolf
exec kitty
exec --no-startup-id autorandr --change
exec --no-startup-id nextcloud
#exec --no-startup-id nm-applet
#exec --no-startup-id caffeine

#My Shortcuts
bindsym $mod+Ctrl+s     exec sh -c 'autorandr --change'
bindsym $mod+z          exec flameshot gui
bindsym $mod+e          exec raiseorlaunch -c Thunar
bindsym $mod+x          exec raiseorlaunch -c kitty -w "5:  "
bindsym $mod+c          exec raiseorlaunch -c code-oss -w "4: ﬏ "
bindsym $mod+s          exec raiseorlaunch -c Spotify -w "10: 阮 "
bindsym $mod+b         	exec raiseorlaunch -c LibreWolf -w "1:  "
bindsym $mod2+b         exec raiseorlaunch -c firefox -w "2:  "
bindsym $mod+m		exec raiseorlaunch -c Vmware -w "3:  "
bindsym $mod+n          exec raiseorlaunch -c Notion -e notion-app -w "6:  "
bindsym $mod+y          exec raiseorlaunch -c FreeTube -w "7:  "
bindsym $mod+g          exec raiseorlaunch -c discord -w "8:  "
bindsym $mod+t          exec raiseorlaunch -c Transmission-gtk -r
bindsym $mod2+z         exec raiseorlaunch -c Bitwarden -r
bindsym $mod2+v         exec raiseorlaunch -c Pavucontrol -r 

#Toggling Sticky keys
bindsym $mod+Shift+s    sticky toggle

#Screen Lock
bindsym $mod+Ctrl+l exec dm-tool lock

# Sreen brightness controls
bindsym $mod2+Ctrl+w exec --no-startup-id xbacklight -inc 10 && pkill -RTMIN+13 i3blocks
bindsym $mod2+Ctrl+s exec --no-startup-id xbacklight -dec 10 && pkill -RTMIN+13 i3blocks

#audio
bindsym $mod2+w                 exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && pkill -RTMIN+10 i3blocks
bindsym $mod2+s                 exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && pkill -RTMIN+10 i3blocks
bindsym $mod2+Shift+s           exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && pkill -RTMIN+10 i3blocks
bindsym XF86AudioMicMute       exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && pkill -RTMIN+10 i3blocks

#Media
bindsym $mod2+x exec playerctl play-pause && pkill -RTMIN+19 i3blocks
bindsym $mod2+d exec playerctl next && pkill -RTMIN+19 i3blocks
bindsym $mod2+a exec playerctl previous && pkill -RTMIN+19 i3blocks

#Display workspace move
bindsym $mod+Ctrl+1 move workspace to output HDMI1
bindsym $mod+Ctrl+2 move workspace to output eDP1

#Scratchpads
bindsym $mod+backslash scratchpad show
bindsym $mod+Shift+backslash move scratchpad
for_window [class=Bitwarden|Transmission-gtk|Pavucontrol] move scratchpad

#window rules
for_window [class=Galculator|feh|Lxappearance|qpdfview|vlc|Galculator] floating enable
for_window [class="VirtualBox Machine"] layout tabbed
for_window [class="Sxiv"] floating enable resize 800 600
for_window [class=Galculator|vlc|Bitwarden|Pavucontrol] sticky enable
for_window [title="Microsoft Teams Notification"] floating enable
